# AGENTS.md

This repository keeps a single set of agent instructions, in
[CLAUDE.md](CLAUDE.md). Read that file.

It covers the stack, the repo layout, how the sequence runtime and the YAML
pipeline fit together, the commands worth knowing, and the working conventions
(workspace aliases, the `seq_` table prefix, per-product Resend secrets, the
suppression rule that must never be skipped in the Durable Object path).
