{
  "name": "@sequencer/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json --noEmit",
    "dev": "wrangler dev --local",
    "deploy": "pnpm -w deploy:prod",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@sentry/cloudflare": "^10.53.1",
    "@sequencer/db": "workspace:*",
    "@sequencer/shared": "workspace:*",
    "drizzle-orm": "^0.43.0",
    "hono": "^4.7.0",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250522.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.14.0"
  }
}
